import UI from "@gh/ui";

export default function App(props) {
  return (
    <UI.Stack>
      <UI.Text variant="h4" bold color="primary">
        About
      </UI.Text>
    </UI.Stack>
  );
}
